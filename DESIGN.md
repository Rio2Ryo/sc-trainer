# SC Trainer 詳細設計書

情報処理安全確保支援士試験（セキスペ）合格のための個人用学習アプリケーション。

- 受験者：1名（開発者本人）
- 目標：令和8年度前期 科目A（2026/10/17〜27）・科目B（2026/11/11〜23）合格
- 科目A-1 免除なし
- 作成日：2026-09-19（科目Aまで28日、科目Bまで65日）

## 0. 設計原則

この4つが他のすべてに優先する。実装judgment に迷ったらここに戻る。

### 原則1：アプリ開発が学習時間を食ってはならない

残り28日で科目A、65日で科目B。 学習に必要な時間は110〜135時間で、これは週10〜12時間ペース。ここにアプリ開発の時間を上乗せする余裕はない。

したがって MVPは1日で完成する規模に限定する。 作り込みたくなったら、その衝動は科目Bの答案演習に向ける。本設計書の「§10 やらないこと」を必ず読むこと。

### 原則2：AIに答えを出させない

OECD Digital Education Outlook 2026 に、汎用チャットボットで練習した学習者はその場の成績は上がったが、後のクローズドブック試験では独学者より最大17%低いスコアだったという結果がある。原因は「有能感の錯覚」と「メタ認知的怠惰」で、流暢なAI出力そのものが引き起こす。

本アプリはAIを採点者・出題者としてのみ使い、解答者として使わない。 UI上、模範解答は自分の答案を確定するまで物理的に開けない構造にする。

### 原則3：一次資料のみを正とする

生成AIは事実や数値を自信満々に誤る。AIに問題を生成させない。 出題は必ずIPA公式または購入した市販教材に由来する。AIの役割は採点・分類・カード整形に限る。

### 原則4：教材はローカルから出さない

購入したPDFには購入者のメールアドレスと著作権情報が埋め込まれる。自炊したスキャンデータは著作権法30条1項の私的複製であり、「個人的に又は家庭内その他これに準ずる限られた範囲」を超える利用は認められない。

教材ファイルをクラウドストレージ・共有ドライブ・公開リポジトリに置かない。 アプリはローカル実行のみ。デプロイしない。

## 1. スコープ

### 対象とする学習行為

| 科目 | 学習時間 | アプリの担当範囲 |
|---|---|---|
| 科目A-1（応用情報範囲、30問50分） | 30〜40h | 誤答の記録とFSRS復習のみ。出題は過去問道場を使う |
| 科目A-2（SC専門、25問40分） | 15h | 同上 |
| 科目B（記述式、150分、4問中2問選択） | 60〜80h | 全面担当。答案作成・AI採点・弱点管理 |

科目Aの出題機能は作らない。過去問道場（Web・無料・全問解説付き）が既に完成度の高い出題エンジンであり、これを再実装する価値はゼロ。アプリは道場が持っていない機能——誤答の長期スケジューリング——だけを担当する。

### 教材の入手経路と形態

| 教材 | 入手 | テキスト層 | アプリでの扱い |
|---|---|---|---|
| IPA 過去問（問題） | 無料DL・自動取得 | なし（全19件で0文字） | ページ画像化してVLMに渡す |
| IPA 解答例 | 無料DL・自動取得 | あり（19/19件） | テキスト抽出 |
| IPA 採点講評 | 無料DL・自動取得 | あり（7/7件） | テキスト抽出。採点基準の中核 |
| 上原本 2026年版 | SEshop PDF版 3,278円 | 購入時に制限事項欄を要確認 | 引く用。全文検索 |
| 重点対策 2026 | 紙 | — | 解く用。アプリ外 |
| 支援士R7 復元答案 | 紙→コンビニスキャン | なし | 画像。採点の基準データ |

IPAの問題PDFにテキスト層がないのは実測済み（科目B問題は36ページで0文字）。これが設計上の最大の制約で、「PDFからテキストを抜いて全部AIに渡す」構成は成立しない。問題は画像、解答例と講評はテキスト、というハイブリッドで組む。

## 2. アーキテクチャ

```
┌─────────────────────────────────────────┐
│  ブラウザ (localhost:5173)               │
│  React + Vite + Tailwind                 │
└───────────────┬─────────────────────────┘
                │ HTTP (localhost only)
┌───────────────▼─────────────────────────┐
│  FastAPI (localhost:8000)                │
│  ├─ /api/kamoku-a   誤答・FSRS           │
│  ├─ /api/kamoku-b   答案・採点            │
│  ├─ /api/materials  教材取り込み          │
│  └─ /api/dashboard  進捗                  │
└───────────────┬─────────────────────────┘
                │
     ┌──────────┼──────────┬──────────────┐
     ▼          ▼          ▼              ▼
 SQLite    data/(PDF)   py-fsrs    Anthropic API
 sc.db     ローカル固定   スケジューラ  (採点のみ)
```

### 技術選定と理由

| 層 | 採用 | 理由 |
|---|---|---|
| バックエンド | Python 3.12 + FastAPI | PDF処理（PyMuPDF）とFSRS（py-fsrs）がPythonに揃っている |
| DB | SQLite（単一ファイル） | 1ユーザー。サーバ不要。バックアップはファイルコピー |
| フロント | React + Vite + Tailwind | 作り慣れた構成を使う。ここで新技術を試さない |
| PDF | PyMuPDF (pymupdf) | テキスト層判定・Markdown化・ページ画像化・図抽出を1ライブラリで賄える |
| 間隔反復 | py-fsrs (open-spaced-repetition) | FSRSの既定パラメータは約7億件のレビュー履歴に基づき、同じ保持率をより少ないレビュー回数で達成する |
| AI | Anthropic API（claude-sonnet-4-6） | 長文ケーススタディの読解と、PDFネイティブ入力に対応 |

新しい技術を学びながら作らないこと。 慣れた構成で最速で動かす。

### ディレクトリ

```
sc-trainer/
├── backend/
│   ├── main.py              FastAPI エントリ
│   ├── db.py                SQLite スキーマ・接続
│   ├── models.py            Pydantic モデル
│   ├── routers/
│   │   ├── kamoku_a.py
│   │   ├── kamoku_b.py
│   │   ├── materials.py
│   │   └── dashboard.py
│   ├── services/
│   │   ├── fetch_ipa.py     IPA過去問の自動取得
│   │   ├── ingest.py        PDF判定・変換
│   │   ├── grader.py        AI採点
│   │   └── scheduler.py     FSRS ラッパー
│   └── prompts/
│       ├── grade.md
│       ├── diagram_check.md
│       └── cards.md
├── frontend/
│   └── src/
│       ├── pages/
│       │   ├── Dashboard.tsx
│       │   ├── KamokuB.tsx      答案作成・採点
│       │   ├── Review.tsx       FSRS復習
│       │   ├── MissLog.tsx      誤答記録
│       │   └── Weakness.tsx     弱点一覧
│       └── components/
├── data/                    ★ .gitignore 対象
│   ├── sc.db
│   ├── ipa/                 自動取得したIPA PDF
│   ├── books/               購入・自炊したPDF
│   └── pages/               ページ画像
└── .gitignore
```

data/ 全体を .gitignore に入れる。教材ファイルをGitに乗せない。

## 3. データモデル

```sql
-- 科目Aの誤答。道場で間違えたものだけを手で登録する
CREATE TABLE miss (
  id           INTEGER PRIMARY KEY,
  subject      TEXT NOT NULL,        -- 'A1' | 'A2'
  field        TEXT,                 -- 'ネットワーク' 等
  question     TEXT NOT NULL,        -- 問題文の要旨（自分の言葉で1行）
  correct      TEXT NOT NULL,        -- 正解
  chose        TEXT,                 -- 自分が選んだ誤答
  why          TEXT,                 -- なぜ間違えたか 1行
  source       TEXT,                 -- 'R7秋 問12' 等
  created_at   TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- FSRS カード。miss から生成される
CREATE TABLE card (
  id           INTEGER PRIMARY KEY,
  miss_id      INTEGER REFERENCES miss(id) ON DELETE CASCADE,
  front        TEXT NOT NULL,        -- 穴埋め文（{{c1::...}} 記法）
  back         TEXT,                 -- 補足。どの問題で間違えたか必須
  tags         TEXT,
  -- FSRS 状態（py-fsrs の Card をそのまま保持）
  due          TIMESTAMP NOT NULL,
  stability    REAL,
  difficulty   REAL,
  state        INTEGER,              -- 0=New 1=Learning 2=Review 3=Relearning
  step         INTEGER,
  last_review  TIMESTAMP
);

CREATE TABLE review_log (
  id          INTEGER PRIMARY KEY,
  card_id     INTEGER REFERENCES card(id) ON DELETE CASCADE,
  rating      INTEGER NOT NULL,      -- 1=Again 2=Hard 3=Good 4=Easy
  reviewed_at TIMESTAMP NOT NULL,
  elapsed_ms  INTEGER
);

-- 科目Bの過去問メタ。fetch_ipa + build_index が投入
CREATE TABLE kamoku_b_question (
  id          INTEGER PRIMARY KEY,
  exam        TEXT NOT NULL,         -- 'R7秋'
  qno         INTEGER NOT NULL,      -- 1..4
  theme       TEXT,                  -- 採点講評から自動抽出
  qs_pdf      TEXT NOT NULL,         -- 問題PDFのパス
  qs_pages    TEXT,                  -- ページ画像ディレクトリ
  ans_md      TEXT,                  -- 解答例 Markdown
  cmnt_md     TEXT,                  -- 採点講評 Markdown
  UNIQUE(exam, qno)
);

-- 答案
CREATE TABLE attempt (
  id           INTEGER PRIMARY KEY,
  question_id  INTEGER REFERENCES kamoku_b_question(id),
  body         TEXT NOT NULL,        -- 設問ごとのJSON {"設問1":"...", ...}
  diagram_mmd  TEXT,                 -- 自分で書き起こしたMermaid
  minutes      INTEGER,              -- 所要時間
  submitted_at TIMESTAMP,
  revealed     BOOLEAN DEFAULT 0     -- 模範解答を開いたか（開くまで採点不可）
);

-- 採点結果
CREATE TABLE grade (
  id           INTEGER PRIMARY KEY,
  attempt_id   INTEGER REFERENCES attempt(id) ON DELETE CASCADE,
  detail       TEXT NOT NULL,        -- 設問ごとの判定JSON
  score_pct    INTEGER,              -- 推定得点率
  next_fix     TEXT NOT NULL,        -- 次回まで直す1点
  graded_at    TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 癖の蓄積。再発検知が目的
CREATE TABLE weakness (
  id           INTEGER PRIMARY KEY,
  label        TEXT NOT NULL UNIQUE,
  count        INTEGER DEFAULT 1,
  last_seen    TIMESTAMP,
  resolved     BOOLEAN DEFAULT 0     -- 3回連続で再発しなければ立てる
);
```

### weakness の初期データ（投入必須）

IPAの採点講評から抽出した実在の誤答パターン。空テーブルから始めない。

| label | 出典 |
|---|---|
| 設問の語尾と答えの形が対応していない | 記述式全般 |
| 字数超過（多くの設問は20〜40字） | 記述式全般 |
| 開発プロセスを問われて運用プロセスの対策に逃げる | R7秋 問1 設問4 の講評でIPAが名指し |
| 問題文中の固有名詞を使わず一般名詞で答える | 記述式全般 |
| 攻撃手法名だけを書いて成立条件や影響を書かない | 記述式全般 |

R7秋問1設問4の講評には、開発プロセスの対策を問うたのに「ログ分析を行う」「IDの棚卸しを行う」「アノマリ検知の仕組みを導入する」といった運用プロセスの対策や、「WAFを導入する」といった製品導入の解答が散見された、と書かれている。

## 4. 取り込みパイプライン

### 4.1 IPA過去問の自動取得

`services/fetch_ipa.py`

IPAの年度別過去問ページ `https://www.ipa.go.jp/shiken/mondai-kaiotu/{year}.html` をスクレイプし、`_sc_` または `_am1_` を含むPDFリンクを収集して `data/ipa/` に保存する。

対象年度は 2023r05, 2024r06, 2025r07。R5秋期から午後Ⅰ・午後Ⅱが統合されて150分1科目になっているため、現行形式の科目Bは R5秋・R6春・R6秋・R7春・R7秋 の5回分＝20問。R5春以前は pm1/pm2 に分かれており形式が違うので、科目Bの演習対象からは外す（知識の補強には使える）。

取得は約45ファイル。サーバ負荷を避けるため各リクエスト間に0.5秒のスリープを入れる。既に存在するファイルはスキップする。

### 4.2 テキスト層の判定

`services/ingest.py`

任意のPDFに対して、最大20ページをサンプリングし、1ページあたりの抽出文字数が120文字以上のページの割合を計算する。50%以上なら text、未満なら image。

- text → PyMuPDFでページ区切り（`<!-- p.N -->`）付きMarkdown化。埋め込み画像（=図表）を200px以上のものだけ別途PNG出力
- image → 200dpiでページ全体をPNG化

この判定は購入・自炊した教材の品質検査としても機能する。自炊してOCRをかけたPDFを投入して image と出たら、OCRが効いていないので解像度（400dpi推奨）や傾きを見直す。

### 4.3 出題テーマ索引の自動生成

`services/build_index.py`

採点講評PDFの冒頭には各問について「問N では，〜について出題した。」という一文が必ず入る。これを正規表現で抽出して `kamoku_b_question.theme` に投入する。全20問のテーマ索引が自動で得られ、4問中2問選択で「確実に取れる2分野」を決める材料になる。

全角数字が混じるため、抽出前に ０１２３４５６７８９ を半角に正規化すること。

## 5. 機能仕様

### 5.1 ダッシュボード（/）

起動時の最初の画面。以下だけを表示する。

- 残日数：科目Aまで / 科目Bまで（大きく）
- 今日のFSRS復習件数（due <= now のカード数）
- 科目B進捗：20問中 演習済み n問、平均得点率
- 再発中の癖 上位3件（count順、resolved=0）
- 次に直す1点（直近のgradeのnext_fix）

グラフは作らない。数字の羅列で十分。

### 5.2 誤答記録（/miss）

過去問道場で間違えた問題を30秒以内に登録できることが唯一の要件。ここが重いとアプリ全体が死ぬ。

- 入力欄5つ（科目、問題要旨、正解、選んだ誤答、なぜ）のみ
- 「なぜ」以外は必須
- 保存後は即座に同じフォームがクリアされて次を入力できる（画面遷移なし）
- スマホでも入力できる幅にする（道場はスマホで回すため）

登録された miss は未カード化状態で溜まる。

### 5.3 カード生成（/miss の一括アクション）

未カード化の miss をまとめてAIに投げ、穴埋めカードを生成する。

- 一度に処理するのは20件まで。 それ以上は質が落ちる
- 生成結果は必ず確認画面で人がレビューしてから確定する。AI生成をそのままDBに入れない
- カード形式はCloze（穴埋め）。最小情報原則に従い、1カード1情報。1文に穴を3つ開けない
- 穴を開けるのは思い出せなかった語のみ。助詞や一般語には開けない
- 選択肢問題は「正解の選択肢を答える」形ではなく「その選択肢が正しい理由を問う」形に変換する
- back にはどの問題で間違えたかを必ず入れる

プロンプトは `backend/prompts/cards.md`。入力にない知識を追加してカードを作らせないことを明示的に禁止する。ハルシネーションがそのまま暗記される。

### 5.4 復習（/review）

FSRSによる間隔反復。

- due <= now のカードを1枚ずつ表示
- 表面（穴埋め）→ クリックで裏面 → 4段階評価（Again / Hard / Good / Easy）
- 評価を review_log に記録し、py-fsrs で次回 due を再計算して card を更新
- キーボードショートカット必須：Space=裏返し、1〜4=評価。マウスを使わせない
- 1セッションの上限は設けない（duになった分だけ出る）

間隔反復の効果は量ではなく継続性から来る。3日間500枚やって1週間やめるより、毎日100枚のほうが良い。 ダッシュボードに連続日数を出して継続を可視化する。

### 5.5 科目B 演習（/kamoku-b/:examId/:qno）★中核

画面は3ペイン。

```
┌──────────────┬──────────────┬─────────────┐
│ 問題          │ 答案          │ 図          │
│ (ページ画像   │ (設問ごとの   │ (Mermaid    │
│  スクロール)  │  テキスト欄)  │  エディタ)  │
│              │              │             │
│              │ [残り 72:14] │ [プレビュー] │
└──────────────┴──────────────┴─────────────┘
              [答案を確定する]
```

問題ペイン：IPAの問題PDFはテキスト層がないため、`data/pages/` のページ画像を縦スクロールで表示する。紙に印刷させない。 本番はCBTで画面上の長文読解になるため、演習時点で画面に慣れる必要がある。

答案ペイン：設問ごとのテキストエリア。キーボード入力であること自体が本番の練習になる（CBTでキーボード入力になるため、変換ミスに注意が要る）。各欄に文字数カウンタを出す。多くの設問は20〜40字。

タイマー：150分のカウントダウン。2問選択なので1問あたり75分。超過しても止めないが、記録する。

図ペイン：構成図をMermaidで自分で書き起こす欄。

これは飾りではない。VLMは図の視覚要素認識やテキスト抽出はできても、ノード数の計数やフロー論理の解釈といった構造推論では苦戦することがベンチマーク（FlowLearn）で示されている。図形で構成された画像の幾何的認識にも限界がある。したがってAIに構成図の解釈を任せるのは最も精度が出ない使い方であり、自分で書き起こすのが正解。しかもその作業自体が本番で必要な図の読み込みそのものになる。

### 5.6 AI採点（/kamoku-b/:examId/:qno/grade）

「答案を確定する」を押すまで、模範解答は開けない。 attempt.revealed が0の間、解答例と採点講評へのAPIは404を返す。フロント側の制御だけにしない。サーバ側で塞ぐ。

確定後の処理：

1. weakness テーブルを読み、既知の癖を把握
2. 問題PDF（またはページ画像）、自分の答案、自分のMermaid、解答例Markdown、採点講評Markdown をAIに渡す
3. 設問ごとに採点させる
4. 結果を grade に保存、weakness を更新

出力形式（設問ごと）：

| 項目 | 内容 |
|---|---|
| 設問 | 設問1(2) |
| 自己答案 | 原文のまま |
| IPA解答例 | 原文のまま |
| 判定 | ○ / △ / × |
| 推定得点 | n/n点 |
| 減点理由 | 1行 |
| 採点講評 | 該当記述の要約、なければ「言及なし」 |

総括：再発した癖 / 新規の癖（最大2件）/ 次回まで直す1点（必ず1件だけ） / 推定得点率（合格ラインは60%）

### 5.7 採点エージェントの制約（backend/prompts/grade.md）

以下をプロンプトに明記する。この制約群が本アプリの価値のほぼ全部。

- 唯一の基準はIPAの解答例と採点講評。 AIが考える「より良い答え」ではない
- 甘く採点しない。 要素が欠けていれば△ではなく×
- 採点講評を必ず読む。 利用者の答案がIPAが名指しした誤答パターンに該当する場合は明示する
- 指摘は1回につき最大3件。 全部指摘すると何も直らない
- 再発は新規指摘より重い。 先に既知の癖の再発をチェックしてから採点する
- 段階的に開示する。 採点後の解説を丁寧にやりすぎない。流暢な出力自体が有能感の錯覚を生む
- 図が読めない、またはMermaidが提出されていない場合は推測で採点せず、利用者に確認する
- 励ますためだけの記述を書かない。 点数に関係ない賞賛は出力しない
- 予想問題を作らない。 CBT化で本番問題は非公開。憶測は学習を歪める

### 5.8 弱点一覧（/weakness）

weakness を count降順で表示。3回連続で再発しなかったものは resolved=1 にして別セクションへ移す。

週次で「分野別の得点率」「本番で選択すべき2分野の推奨」を出す機能を付けてもよいが、MVPには含めない。

## 6. API仕様（抜粋）

```
GET    /api/dashboard                    ダッシュボード用の集計
POST   /api/miss                         誤答1件を登録
GET    /api/miss?carded=false            未カード化の誤答
POST   /api/miss/generate-cards          一括カード生成（最大20件）→ 確認用に返すだけ
POST   /api/cards                        レビュー済みカードを確定保存
GET    /api/review/next                  due のカードを1枚
POST   /api/review/:cardId               rating を記録し次回 due を計算
GET    /api/kamoku-b/questions           20問の一覧（theme付き）
GET    /api/kamoku-b/:id/pages           問題のページ画像URL配列
POST   /api/kamoku-b/:id/attempt         答案を保存（確定）
GET    /api/kamoku-b/:id/answer          解答例＋講評（revealed=1 のときのみ200）
POST   /api/kamoku-b/attempt/:id/grade   AI採点を実行
GET    /api/weakness                     弱点一覧
POST   /api/materials/fetch-ipa          IPA過去問を取得
POST   /api/materials/ingest             任意PDFを投入（判定→変換）
```

## 7. 非機能要件

| 項目 | 要件 |
|---|---|
| 実行環境 | localhost のみ。0.0.0.0 にバインドしない |
| 認証 | なし（ローカル単一ユーザー） |
| デプロイ | しない |
| バックアップ | data/sc.db を日次でコピー。教材PDFはバックアップ不要（再取得可能） |
| Git | data/ 全体を .gitignore。教材ファイルを絶対にコミットしない |
| APIキー | .env に置き、.gitignore に入れる |
| 著作権 | 自炊データ・購入PDFは私的複製の範囲。社内共有・再配布・クラウド同期を行わない |
| 試験後 | 本番の試験問題をアプリに入力しない。 CBT化により問題は非公開・第三者開示禁止 |

## 8. AIコスト見積もり

科目B 1問の採点で、問題ページ画像（10〜15枚）＋解答例＋講評＋答案を投げる。20問×2周＝40回。

これは推測ですが、1回あたり数十円規模、合計で2,000〜3,000円程度に収まる見込みです。実際の単価は実行して測ってください。上振れした場合は、問題ページ画像を全ページ送らず設問に関係するページだけ送る最適化を入れる余地があります。

## 9. 実装順序

### MVP（1日・これだけで学習は回る）

1. SQLite スキーマ作成、weakness の初期5件を投入
2. fetch_ipa.py：IPA過去問45ファイルの取得
3. ingest.py：テキスト層判定・Markdown化・ページ画像化
4. build_index.py：採点講評からテーマ索引を生成
5. 科目B演習画面（3ペイン）＋ 答案保存
6. AI採点（grade.md の制約9項目を必ず実装）
7. ダッシュボード（残日数・進捗・次に直す1点）

この時点で科目Bの演習ループが回る。ここで一度止めて、1週間使うこと。

### Phase 2（週末半日・科目A対策が始まる9月末までに）

- 誤答記録フォーム（30秒で入力できること）
- FSRS復習画面（キーボードショートカット必須）
- カード生成＋人によるレビュー確認画面

### Phase 3（余裕があれば。なければ作らない）

- 弱点の週次レビュー
- 分野別得点率と「選択すべき2分野」の推奨

## 10. やらないこと

作りたくなるが、作ってはいけないもの。すべて学習時間を奪って合格から遠ざける。

- ❌ 科目Aの出題エンジン — 過去問道場で足りる。再実装は純粋な時間の浪費
- ❌ AIによる予想問題・擬似問題の生成 — 生成AIは事実や数値を自信満々に誤り、誤った知識をそのまま暗記することになる。擬似問題は市販問題集の補完に留めるのが現実的とされている
- ❌ AIに構成図を解釈させる機能 — VLMは図の構造推論で苦戦する。自分でMermaidに書き起こす
- ❌ ColPali等のマルチベクトル視覚検索 — 対象は20問5回分しかない。検索が課題ではないので過剰
- ❌ マルチユーザー対応・認証・デプロイ — 受験者は1名
- ❌ モバイルアプリ化 — 誤答記録がスマホのブラウザで動けば十分
- ❌ 学習時間の自動計測・ポモドーロ・ゲーミフィケーション — 点に繋がらない
- ❌ グラフとアニメーション — 数字で足りる
- ❌ AIが答えを先に見せるモード — 原則2に反する

## 11. 受け入れ基準

MVPが完成したと言える条件。

- POST /api/materials/fetch-ipa でIPA公式PDF45ファイルが取得できる
- ingest が問題PDFを image、解答例と講評を text と正しく判定する
- 20問のテーマ索引が採点講評から自動生成される
- 科目B演習画面で、問題を画面で読み、答案をキーボードで書き、150分タイマーが動く
- 答案を確定するまで、解答例APIが404を返す
- AI採点が設問ごとの判定と「次回まで直す1点」を返す
- 採点結果が weakness に反映され、再発時に「再発」と表示される
- data/ がGitに含まれていない

## 12. Claude Code への渡し方

このファイルをリポジトリ直下に DESIGN.md として置き、以下を最初の指示にする。

```
DESIGN.md を読んで、§9 の MVP（項目1〜7）だけを実装してください。
§10「やらないこと」に挙がっているものは、提案もしないでください。
§0 の設計原則4つは、実装judgment に迷ったときの判断基準です。
まず §9 の1〜4（データ層と取り込みパイプライン）を実装して、
動作確認が取れてから5〜7（UIと採点）に進んでください。
```

Phase 2以降は、MVPを1週間実際に使ってから着手すること。使う前に作り込むと、使わない機能を作ることになる。
